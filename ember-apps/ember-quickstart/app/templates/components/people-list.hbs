<h2>{{title}}</h2>

<!-- Turn the array of string into HTML -->
<ul>
  {{#each people as |person|}}
    <!-- click Events -->
    <li {{action "showPerson" person}}>{{person}} </li>
  {{/each}}
</ul>
